#!/bin/bash
# Sync zettelkasten notes to Obsidian vault
# Usage: ./scripts/sync-to-obsidian

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR" && uv run python scripts/sync-to-obsidian.py "$@"
